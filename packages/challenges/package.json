{
  "name": "@faultline/challenges",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && mkdir -p dist/levels && cp src/levels/*.level.json dist/levels/",
    "verify": "pnpm --filter @faultline/core build && pnpm run build && node scripts/verify-tiny-api.mjs && node scripts/verify-url-shortener.mjs && node scripts/verify-config-hash.mjs && node scripts/verify-workload-affinity.mjs && node scripts/verify-level-profile-schema.mjs && node scripts/verify-url-shortener-level-profile.mjs && node scripts/verify-premiere-night-level-profile.mjs"
  },
  "dependencies": {
    "@faultline/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  }
}
