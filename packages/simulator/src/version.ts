/**
 * Competition-affecting simulator semantics version.
 * Bump when pass/fail, metrics, or cost rules change in a way that would
 * invalidate prior official challenge_versions. Publishing a new challenge
 * version is required alongside a bump.
 */
export const SIMULATOR_VERSION = "1";
