# AI

Embedded AI will interpret simulator evidence and help challenge a human design. It never invents metrics or decides correctness; the deterministic simulator does.

`packages/agent-capabilities` will define adapter-neutral semantic capabilities. The embedded AI and WebMCP adapters will consume that layer instead of duplicating business logic. No AI integration is implemented yet.
