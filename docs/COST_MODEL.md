# Cost model

Cost is a deterministic simulation constraint, not presentation-only metadata. The simulator will calculate it from the canonical architecture and scenario inputs so browser and server agree.

Pricing rules, units, budgets, and component cost data are not implemented yet.
